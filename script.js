// document.getElementById('phoneForm').addEventListener('submit', function(event) {
//     event.preventDefault();

// });

window.onload = function() {
    if (window.jQuery) {  
        console.log("Fuck! I worked ")
    } else {
        // jQuery is not loaded
        alert("Doesn't Work");
    }
}